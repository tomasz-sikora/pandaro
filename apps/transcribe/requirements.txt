fastapi>=0.115.0
uvicorn[standard]>=0.34.0
faster-whisper>=1.1.0
pyannote.audio>=3.3.2
speechbrain>=1.0.2
scikit-learn>=1.6.0
# VibeVoice requires transformers 4.x (5.x removed tokenization_qwen2_fast)
transformers>=4.47.0,<5.0.0
diffusers>=0.30.0
# int8 quantization for VibeVoice — halves VRAM from ~18 GB to ~9 GB
bitsandbytes>=0.44.0
pydub>=0.25.1
python-multipart>=0.0.20
httpx>=0.28.0
numpy>=1.26.0
soundfile>=0.13.0
librosa>=0.10.0
huggingface-hub>=0.27.0
scipy>=1.14.0
# Language detection for VibeVoice output
langdetect>=1.0.9
langid>=1.1.6
# VibeVoice-ASR backbone dependencies
accelerate>=1.2.0
sentencepiece>=0.2.0
tiktoken>=0.8.0
einops>=0.8.0
# Audio feature extraction
datasets>=3.0.0
# Nemotron 3.5 ASR (NVIDIA NeMo) — install separately, not via pip install -r:
#   apt-get update && apt-get install -y libsndfile1 ffmpeg
#   pip install Cython packaging
#   pip install git+https://github.com/NVIDIA/NeMo.git@main#egg=nemo_toolkit[asr]
# omegaconf is required by NeMo and used for override_config
omegaconf>=2.3.0

