# Core runtime dependencies (no GPU / ML required).
# These let the API server, orchestrator and all pure logic run and be tested.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
python-multipart>=0.0.9
httpx>=0.27
structlog>=24.1
numpy>=1.26
